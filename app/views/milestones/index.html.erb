<h1>All Milestones</h1>
<a href="/milestones/new">New Milestone</a>

<% @milestones.each do |milestone| %>
  <h3>Name: <%= milestone.name %></h3>
  <h3>Milestone: <%=milestone.milestone%></h3>
  <div>
  <a href="/milestones/<%= milestone.id %>">See Details</a>
  </div>
<% end %>